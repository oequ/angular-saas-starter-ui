# adapters-mock

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build adapters-mock` to build the library.

## Running unit tests

Run `nx test adapters-mock` to execute the unit tests via [Vitest](https://vitest.dev/).
